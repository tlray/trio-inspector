# Personal builds contain medical data and/or access tokens — never commit these
trio_data.json
*-snapshot.html
*-live.html

# Local dev/test dependencies (e.g. Playwright for browser tests)
node_modules/
